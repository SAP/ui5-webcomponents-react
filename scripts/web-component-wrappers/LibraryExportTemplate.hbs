import { {{name}} } from '../webComponents/{{name}}';
import type { {{name}}PropTypes, {{name}}DomRef } from '../webComponents/{{name}}';

export { {{name}} };
export type { {{name}}PropTypes, {{name}}DomRef };
