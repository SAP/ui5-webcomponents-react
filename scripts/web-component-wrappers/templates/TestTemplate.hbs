import { cypressPassThroughTestsFactory } from '@/cypress/support/utils';
import { {{name}} } from './index';

describe('{{name}}', () => {
  it('Basic Test (generated)', () => {
    cy.mount(<{{name}} />);
    cy.get('[{{{tagname}}}]').should('exist');
  });

  cypressPassThroughTestsFactory({{name}});
});
