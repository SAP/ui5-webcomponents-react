import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { DocsHeader } from '@shared/stories/DocsHeader';
import { DocsCommonProps } from '@shared/stories/DocsCommonProps';
import { ScatterChart } from '@ui5/webcomponents-react-charts/dist/ScatterChart';
import { scatterColorDataSet, scatterComplexDataSet } from '../../resources/DemoProps';

<Meta
  title="Charts /  ScatterChart"
  component={ScatterChart}
  argTypes={{
    ...DocsCommonProps,
    children: {
      control: { disable: true }
    }
  }}
/>

<DocsHeader />

## Example

<Canvas>
  <Story
    name="Default"
    args={{
      dataset: scatterComplexDataSet,
      style: { width: '100%' },
      measures: [
        {
          accessor: 'users',
          label: 'Users',
          axis: 'x'
        },
        {
          accessor: 'sessions',
          label: 'Sessions',
          formatter: (val) => `${val}k`,
          axis: 'y'
        },
        {
          accessor: 'volume',
          axis: 'z'
        }
      ]
    }}
  >
    {(props) => <ScatterChart {...props} />}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Default" />

<br />
<br />

## More Examples

### With Custom Color

<Canvas>
  <Story
    name="With Custom Color"
    args={{
      dataset: scatterColorDataSet,
      style: { width: '100%' },
      measures: [
        {
          accessor: 'users',
          label: 'Users',
          axis: 'x'
        },
        {
          accessor: 'sessions',
          label: 'Sessions',
          axis: 'y'
        },
        {
          accessor: 'volume',
          axis: 'z'
        }
      ]
    }}
  >
    {(props) => <ScatterChart {...props} />}
  </Story>
</Canvas>

### Loading Placeholder

<Canvas>
  <Story name="Loading Placeholder">
    <ScatterChart style={{ width: '100%' }} measures={[]} />
  </Story>
</Canvas>
