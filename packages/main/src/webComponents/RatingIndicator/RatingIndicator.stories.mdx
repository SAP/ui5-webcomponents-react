import { Title, Subtitle, Description, Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { RatingIndicator } from '@ui5/webcomponents-react/lib/RatingIndicator';
import { createSelectArgTypes } from '@shared/stories/createSelectArgTypes';

<Meta
  title="UI5 Web Components / RatingIndicator"
  component={RatingIndicator}
  args={{
    value: 3
  }}
/>

<Title />
<Subtitle />
<Description />

<Preview>
  <Story name="Default">
    {(args) => {
      return <RatingIndicator {...args} />;
    }}
  </Story>
</Preview>

<Props story="Default" components={{ RatingIndicator }} />
