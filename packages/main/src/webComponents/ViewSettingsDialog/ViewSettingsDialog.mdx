import { ControlsWithNote, DocsHeader, Footer } from '@sb/components';
import SubcomponentsSection from '@sb/docs/SubcomponentsSection.md?raw';
import { ArgTypes, Canvas, Description, Markdown, Meta } from '@storybook/blocks';
import { FilterItem } from '../FilterItem/index';
import { FilterItemOption } from '../FilterItemOption/index';
import { SortItem } from '../SortItem/index';
import * as ComponentStories from './ViewSettingsDialog.stories';
import { excludePropsForAbstract } from '@sb/utils';

<Meta of={ComponentStories} />

<DocsHeader since="0.20.0" />

<br />

## Example

<Canvas of={ComponentStories.Default} />

## Properties

<ControlsWithNote of={ComponentStories.Default} />

## Opening ViewSettingsDialog

You can open and close the `ViewSettingsDialog` component in a declarative way using the `open` prop.

```jsx
const ViewSettingsDialogComponent = () => {
  const [showVSD, setShowVSD] = useState(false);
  return (
    <>
      <Button
        onClick={() => {
          setShowVSD(true);
        }}
      >
        Open ViewSettingsDialog
      </Button>
      <ViewSettingsDialog
        open={showVSD}
        onClose={() => {
          setShowVSD(false);
        }}
        sortItems={
          <>
            <SortItem text="Name" />
            <SortItem text="Position" />
            <SortItem text="Company" />
            <SortItem text="Department" />
          </>
        }
      />
    </>
  );
};
```

<Markdown>{SubcomponentsSection}</Markdown>

## SortItem

<Description of={SortItem} />
<ArgTypes exclude={excludePropsForAbstract} of={SortItem} />

## FilterItem

<Description of={FilterItem} />
<ArgTypes exclude={excludePropsForAbstract} of={FilterItem} />

## FilterItemOption

<Description of={FilterItemOption} />
<ArgTypes exclude={excludePropsForAbstract} of={FilterItemOption} />

<Footer />
