import { ArgTypesWithNote, ControlsWithNote, DocsHeader, Footer } from '@sb/components';
import SubcomponentsSection from '@sb/docs/SubcomponentsSection.md?raw';
import { Canvas, Description, Markdown, Meta } from '@storybook/blocks';
import { CardHeader } from '../CardHeader';
import * as ComponentStories from './Card.stories';
import { AnalyticalCardHeader } from '../../components/AnalyticalCardHeader/index';
import { NumericSideIndicator } from '../../components/NumericSideIndicator/index';

<Meta of={ComponentStories} />

<DocsHeader subComponents={['AnalyticalCardHeader', 'NumericSideIndicator']} mergeSubComponents />

<br />

## Example

<Canvas of={ComponentStories.Default} />

## Properties

<ControlsWithNote of={ComponentStories.Default} />

## More Examples

### Card with AnalyticalCardHeader

**Note:** The `AnalyticalCardHeader` is a pure React component developed exclusively in the ui5-webcomponents-react repo.

<Canvas of={ComponentStories.WithAnalyticalCardHeader} />
<ControlsWithNote of={ComponentStories.WithAnalyticalCardHeader} />

<Markdown>{SubcomponentsSection}</Markdown>

## CardHeader

<Description of={CardHeader} />
<ArgTypesWithNote of={CardHeader} />

## AnalyticalCardHeader

<Description of={AnalyticalCardHeader} />

**Note:** The `AnalyticalCardHeader` is a pure React component developed exclusively in the ui5-webcomponents-react repo.

<ArgTypesWithNote of={AnalyticalCardHeader} />

## NumericSideIndicator

<Description of={NumericSideIndicator} />

**Note:** The `NumericSideIndicator` is a pure React component developed exclusively in the ui5-webcomponents-react repo.
It should only be used inside the `AnalyticalCardHeader`.

<ArgTypesWithNote of={NumericSideIndicator} />

<Footer />
