import { Canvas, Meta, Story } from '@storybook/blocks';
import { FileUploader } from './index';
import { Button } from '../Button';
import { FlexBox } from '../../components/FlexBox';
import { FlexBoxAlignItems } from '../../enums/FlexBoxAlignItems';
import { Badge } from '../Badge';
import { Avatar } from '../Avatar';
import { ValueState } from '../../enums/ValueState';
import { ArgsTableWithNote, DocsHeader, DomRefTable, Footer } from '@sb/components';
import uploadIcon from '@ui5/webcomponents-icons/dist/upload.js';
import FileUploaderDomRef from './FileUploaderDomRef.json';

<Meta
  title="Inputs / FileUploader"
  component={FileUploader}
  argTypes={{
    children: { control: { disable: true } },
    valueStateMessage: { control: { disable: true } }
  }}
  args={{
    valueState: ValueState.None
  }}
/>

<DocsHeader since="0.9.0" />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      return <FileUploader {...args} />;
    }}
  </Story>
</Canvas>

## Properties

<ArgsTableWithNote story="Default" />

<DomRefTable rows={FileUploaderDomRef} />

<br />

# More Examples

<br />

## FileUploader with Buttons

<br />

### Upload single file

<Canvas>
  <Story name="With Buttons">
    {(args) => {
      return (
        <FileUploader {...args}>
          <Button>Upload single file</Button>
        </FileUploader>
      );
    }}
  </Story>
</Canvas>

### Upload multiple files

<Canvas>
  <FileUploader multiple>
    <Button>Upload multiple files</Button>
  </FileUploader>
</Canvas>

<br />

## Custom FileUploader

### FileUploader without input field

<Canvas>
  <Story name="Custom FileUploader">
    {(args) => {
      return (
        <FileUploader hideInput>
          <Button>Upload file</Button>
        </FileUploader>
      );
    }}
  </Story>
</Canvas>

<Footer />
