import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ProgressIndicator } from '@ui5/webcomponents-react/lib/ProgressIndicator';
import { ValueState } from '@ui5/webcomponents-react/lib/ValueState';

import { createSelectArgTypes } from '@shared/stories/createSelectArgTypes';
import { DocsHeader } from '@shared/stories/DocsHeader';

<Meta
  title="UI5 Web Components / ProgressIndicator"
  component={ProgressIndicator}
  argTypes={{
    ...createSelectArgTypes({ valueState: ValueState })
  }}
  args={{
    valueState: ValueState.None
  }}
/>

<DocsHeader />

<Canvas>
  <Story name="Default">
    {(args) => {
      return <ProgressIndicator {...args} />;
    }}
  </Story>
</Canvas>

<ArgsTable story="." />
<h3>Responsive Behavior</h3>
You can change the size of the Rating Indicator by changing its <code>width</code> or <code>height</code> CSS
properties. <br />
Example: <code>&lt;ProgressIndicator style="height: 2rem; width: 6rem;">&lt;/ProgressIndicator></code>

For the <code>ProgressIndicator</code>
