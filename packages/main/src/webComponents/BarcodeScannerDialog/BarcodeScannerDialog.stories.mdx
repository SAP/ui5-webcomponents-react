import { ArgsTable, Canvas, Markdown, Meta, Story } from '@storybook/blocks';
import { BarcodeScannerDialog } from './index';
import { Button } from '../Button';
import { DocsHeader } from '@docs/DocsHeader';
import { Footer } from '@docs/Footer';
import { useRef } from 'react';
import BarcodeScannerDialogMethods from './BarcodeScannerDialogMethods.md?raw';

<Meta title="Inputs / BarcodeScannerDialog" component={BarcodeScannerDialog} />

<DocsHeader since="0.18.0" />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      const ref = useRef(null);
      return (
        <>
          <Button
            onClick={() => {
              ref.current.show();
            }}
          >
            Open BarcodeScannerDialog
          </Button>
          <BarcodeScannerDialog {...args} ref={ref} />
        </>
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTable story="." />

<Markdown>{BarcodeScannerDialogMethods}</Markdown>

<Footer />
