import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { BarcodeScannerDialog } from './index';
import { Button } from '../Button';
import { DocsHeader } from '@docs/DocsHeader';
import { Footer } from '@docs/Footer';
import { useRef } from 'react';
import BarcodeScannerDialogMethods from './BarcodeScannerDialogMethods.md';

<Meta title="Inputs / BarcodeScannerDialog" component={BarcodeScannerDialog} />

<DocsHeader since="0.18.0" />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      const ref = useRef();
      return (
        <>
          <Button
            onClick={() => {
              ref.current.show();
            }}
          >
            Open BarcodeScannerDialog
          </Button>
          <BarcodeScannerDialog {...args} ref={ref} />
        </>
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTable story="." />

<BarcodeScannerDialogMethods />

<Footer />
