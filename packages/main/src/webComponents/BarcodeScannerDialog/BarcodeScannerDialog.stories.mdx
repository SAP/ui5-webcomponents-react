import { ArgsTable, Canvas, Meta, Story } from '@storybook/blocks';
import { BarcodeScannerDialog } from './index';
import { Button } from '../Button';
import { DocsHeader, DomRefTable, Footer } from '@sb/components';
import { useRef } from 'react';
import BarcodeScannerDomRef from './BarcodeScannerDialogDomRef.json';

<Meta title="Inputs / BarcodeScannerDialog" component={BarcodeScannerDialog} />

<DocsHeader since="0.18.0" />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      const ref = useRef(null);
      return (
        <>
          <Button
            onClick={() => {
              ref.current.show();
            }}
          >
            Open BarcodeScannerDialog
          </Button>
          <BarcodeScannerDialog {...args} ref={ref} />
        </>
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTable of="^" />

<DomRefTable rows={BarcodeScannerDomRef} />

<Footer />
