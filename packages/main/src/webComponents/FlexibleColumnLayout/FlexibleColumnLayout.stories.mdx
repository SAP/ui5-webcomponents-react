import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { FlexibleColumnLayout } from '@ui5/webcomponents-react/lib/FlexibleColumnLayout';
import { FCLLayout } from '@ui5/webcomponents-react/lib/FCLLayout';

import { createSelectArgTypes } from '@shared/stories/createSelectArgTypes';
import { DocsHeader } from '@shared/stories/DocsHeader';

<Meta
  title="UI5 Web Components / FlexibleColumnLayout"
  component={FlexibleColumnLayout}
  argTypes={{
    ...createSelectArgTypes({ layout: FCLLayout }),
    endColumn: { control: { disable: true } },
    midColumn: { control: { disable: true } },
    startColumn: { control: { disable: true } }
  }}
  args={{
    layout: FCLLayout.OneColumn
  }}
/>

<DocsHeader />

<Canvas>
  <Story name="Default">
    {(args) => {
      return <FlexibleColumnLayout {...args} />;
    }}
  </Story>
</Canvas>

<ArgsTable story="." />
<div style={{fontFamily:'var(--sapFontFamily)', fontSize:'var(--sapFontSize)', color:'var(--sapTextColor)'}}>
<h3>Usage</h3>
Use this component for applications that need to display several logical levels of related information side by side
(e.g. list of items, item, sub-item, etc.). The Component is flexible in a sense that the application can focus the
user's attention on one particular column.
</div>