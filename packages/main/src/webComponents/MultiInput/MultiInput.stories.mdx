import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { MultiInput } from '@ui5/webcomponents-react/dist/MultiInput';
import { InputType } from '@ui5/webcomponents-react/dist/InputType';
import { ValueState } from '@ui5/webcomponents-react/dist/ValueState';
import '@ui5/webcomponents-icons/dist/employee.js';
import { Token } from '@ui5/webcomponents-react/dist/Token';
import { createSelectArgTypes } from '@shared/stories/createSelectArgTypes';
import { DocsHeader } from '@shared/stories/DocsHeader';
import { DocsCommonProps } from '@shared/stories/DocsCommonProps';

<Meta
  title="Inputs / MultiInput"
  component={MultiInput}
  subcomponents={{ Token }}
  argTypes={{
    ...createSelectArgTypes({ type: InputType, valueState: ValueState }),
    ...DocsCommonProps,
    tokens: { control: { disable: true } },
    children: { control: { disable: true } },
    icon: { control: { disable: true } },
    valueStateMessage: { control: { disable: true } }
  }}
  args={{
    type: InputType.Text,
    valueState: ValueState.None,
    style: { width: '400px' },
    className: '',
    tooltip: '',
    slot: '',
    ref: null
  }}
/>

<DocsHeader />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      return (
        <MultiInput
          {...args}
          tokens={
            <>
              <Token text="Argentina" />
              <Token text="Bulgaria" />
              <Token text="England" />
              <Token text="Finland" />
              <Token text="Germany" />
              <Token text="Hungary" />
              <Token text="Italy" />
              <Token text="Luxembourg" />
              <Token text="Mexico" />
              <Token text="Philippines" />
              <Token text="Sweden" />
              <Token text="USA" />
            </>
          }
        />
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTable story="." />

## Methods

This component exposes public methods. You can invoke them directly on the instance of the component, e.g. by using React Refs.

| Name       | Parameters | Description                                |
| ---------- | ---------- | ------------------------------------------ |
| **isOpen** | &mdash;    | Checks if the value state popover is open. |

## Render SuggestionItems

The `MultiInput` component supports rendering `SuggestionItems`. An example on how to use them can be found [here](?path=/docs/inputs-input--with-suggestion-item).
