import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { ArgsTableWithNote } from '@docs/ArgsTableWithNote';
import { TabContainer } from './index';
import { Tab } from '../Tab';
import { TabSeparator } from '../TabSeparator';
import { TabLayout } from '../../enums/TabLayout';
import { SemanticColor } from '../../enums/SemanticColor';
import { DocsHeader } from '@docs/DocsHeader';
import { Footer } from '@docs/Footer';
import Description from './TabContainerDescription.md';
import '@ui5/webcomponents-icons/dist/menu.js';
import '@ui5/webcomponents-icons/dist/activities.js';
import '@ui5/webcomponents-icons/dist/add.js';
import '@ui5/webcomponents-icons/dist/calendar.js';
import '@ui5/webcomponents-icons/dist/settings.js';
import '@ui5/webcomponents-icons/dist/sys-monitor.js';

<Meta
  title="Layouts & Floorplans / TabContainer"
  component={TabContainer}
  subcomponents={{ Tab, TabSeparator }}
  argTypes={{
    children: { control: { disable: true } },
    overflowButton: { control: { disable: true } }
  }}
  args={{
    tabLayout: TabLayout.Standard
  }}
/>

<DocsHeader />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      return (
        <TabContainer {...args}>
          <Tab text="Tab One" icon="menu" selected additionalText="5">
            Content Tab 1
          </Tab>
          <Tab text="Tab Two" icon="activities" additionalText="20">
            Content Tab 2
          </Tab>
          <Tab text="Tab Three" icon="add">
            Content Tab 3
          </Tab>
          <Tab text="Tab Four" icon="employee">
            Content Tab 4
          </Tab>
          <Tab text="Tab Five" icon="settings">
            Content Tab 5
          </Tab>
        </TabContainer>
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTableWithNote story="." />

<Description />

# More Examples

<br />

## TabContainer with customizable Tab

The <code>Tab</code> represents a selectable item inside a <code>TabContainer</code>. It defines both the item in the
tab strip (top part of the <code>TabContainer</code>) and the content that is presented to the user once the tab is
selected

<Canvas>
  <Story
    name="with customizable Tab"
    args={{
      additionalText: '10',
      disabled: false,
      icon: 'sys-monitor',
      selected: true,
      design: SemanticColor.Positive,
      text: 'Customizable Tab',
      children: 'Tab Content'
    }}
    argTypes={{
      additionalText: { description: `Represents the "additionalText" text, which is displayed in the tab filter.` },
      disabled: { description: `Enabled items can be selected.` },
      icon: {
        description: `Defines the icon source URI to be displayed as graphical element within the <code>Tab</code>. The SAP-icons font provides numerous built-in icons. See all the available icons in the <ui5-link target="_blank" href="https://openui5.hana.ondemand.com/test-resources/sap/m/demokit/iconExplorer/webapp/index.html" class="api-table-content-cell-link">Icon Explorer</ui5-link>.`
      },
      selected: { description: `Specifies if the <code>Tab</code> is selected.` },
      design: {
        options: Object.values(SemanticColor),
        control: { type: 'radio' }
      },
      stableDomRef: { description: `Defines the stable selector that you can use via getStableDomRef method.` },
      text: { description: `The text to be displayed for the item.` },
      children: { description: `Defines the tab content.`, control: { disable: false } },
      collapsed: { table: { disable: true } },
      fixed: { table: { disable: true } },
      showOverflow: { table: { disable: true } },
      tabLayout: { table: { disable: true } },
      overflowButton: { table: { disable: true } },
      onTabSelect: { table: { disable: true } }
    }}
  >
    {(args) => {
      return (
        <TabContainer {...args}>
          <Tab {...args} />
          <Tab text="Tab 2" icon="activities">
            Content Tab 2
          </Tab>
          <Tab text="Tab 3" icon="add">
            Content Tab 3
          </Tab>
          <Tab text="Tab 4" icon="employee">
            Content Tab 4
          </Tab>
          <Tab text="Tab 5" icon="settings">
            Content Tab 5
          </Tab>
        </TabContainer>
      );
    }}
  </Story>
</Canvas>

<ArgsTableWithNote story="with customizable Tab" />

<br />

## TabContainer with TabSeparator

The <code>TabSeparator</code> represents a vertical line to separate tabs inside a <code>TabContainer</code>

<Canvas>
  <Story name="with TabSeparator">
    {(args) => {
      return (
        <TabContainer {...args}>
          <Tab text="Tab One" icon="menu" selected additionalText="5">
            Content Tab 1
          </Tab>
          <TabSeparator />
          <Tab text="Tab Two" icon="activities" additionalText="20">
            Content Tab 2
          </Tab>
        </TabContainer>
      );
    }}
  </Story>
</Canvas>

<Footer />
