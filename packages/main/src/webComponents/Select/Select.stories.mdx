import { Canvas, Markdown, Meta, Story } from '@storybook/blocks';
import { ArgsTableWithNote } from '@docs/ArgsTableWithNote';
import { Select } from './index';
import { ValueState } from '../../enums/ValueState';
import { Option } from '../Option';
import { DocsHeader } from '@docs/DocsHeader';
import { Footer } from '@docs/Footer';
import Description from './SelectDescription.md?raw';
import SubcomponentsSection from '@sb/docs/SubcomponentsSection.md?raw';
import OptionDescription from '../Option/OptionDescription.md?raw';
import SelectMethods from './SelectMethods.md?raw';

<Meta
  title="Inputs / Select"
  component={Select}
  subcomponents={{ Option }}
  argTypes={{
    children: { control: { disable: true } },
    valueStateMessage: { control: { disable: true } }
  }}
  args={{
    valueState: ValueState.None
  }}
/>

<DocsHeader since="0.4.0" />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      return (
        <Select {...args}>
          <Option>Option 1</Option>
          <Option>Option 2</Option>
          <Option>Option 3</Option>
          <Option>Option 4</Option>
          <Option>Option 5</Option>
        </Select>
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTableWithNote story="." />

<Markdown>{Description}</Markdown>

<Markdown>{SelectMethods}</Markdown>

## Recipes

### Get the ID of the selected item with `onChange`

With the help of the HTML5 `data-` API you can define any primitive value (like numbers, strings)
as an attribute on an element.
This data will be available on the `onChange` event as part of the `dataset` object of the `selectedOption`:

```jsx
const data = [
  { id: 1, text: 'Option 1' },
  { id: 2, text: 'Option 2' }
];

const onChange = (event) => {
  // event.detail.selectedOption is a reference to the selected HTML Element
  // dataset contains all attributes that were passed with the data- prefix.
  console.log(event.detail.selectedOption.dataset.id);
};

<Select onChange={onChange}>
  {data.map((item) => (
    <Option key={item.id} data-id={item.id}>
      {item.text}
    </Option>
  ))}
</Select>;
```

<Markdown>{SubcomponentsSection}</Markdown>

## Option

<Markdown>{OptionDescription}</Markdown>

<Footer />
