import { Canvas, Markdown, Meta, Story } from '@storybook/blocks';
import { TimePicker } from './index';
import { ValueState } from '../../enums/ValueState';
import { ArgsTableWithNote, DocsHeader, DomRefTable, Footer } from '@sb/components';
import TimePickerDomRef from './TimePickerDomRef.json';

<Meta
  title="Inputs / TimePicker"
  component={TimePicker}
  argTypes={{
    valueStateMessage: { control: { disable: true } }
  }}
  args={{
    valueState: ValueState.None
  }}
/>

<DocsHeader since="0.9.0" />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(args) => {
      return <TimePicker {...args} />;
    }}
  </Story>
</Canvas>

## Properties

<ArgsTableWithNote story="Default" />

<DomRefTable rows={TimePickerDomRef} />

<Footer />
