import { PopoverPlacementType } from '@ui5/webcomponents-react/dist/PopoverPlacementType';
import { TitleLevel } from '@ui5/webcomponents-react/dist/TitleLevel';
import { Button } from '@ui5/webcomponents-react/dist/Button';
import { VariantManagement } from '@ui5/webcomponents-react/dist/VariantManagement';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { DocsHeader } from '@shared/stories/DocsHeader';
import { VariantItem } from './VariantItem';
import { useState } from '@storybook/addons';
import { useReducer } from 'react';

<Meta
  title="Inputs / VariantManagement"
  component={VariantManagement}
  argTypes={{
    ref: {
      type: null
    }
  }}
  args={{
    placement: PopoverPlacementType.Bottom,
    level: TitleLevel.H4
  }}
/>

<DocsHeader />

<br />

## Example

<Canvas>
  <Story name="Default">
    {(props) => {
      const [favs, setFavs] = useReducer((prev) => !prev, false);
      return (
        <>
          <VariantManagement
            {...props}
            closeOnItemSelect={false}
            onSaveAs={(e) => {}}
            onSelect={(e) => {
              console.log(e);
            }}
          >
            <VariantItem labelReadOnly favorite={favs} data-test={'Hello there'} selected>
              Variant 1
            </VariantItem>
            <VariantItem isDefault author="Guybrush Threepwood" favorite>
              Variant 2 - Default
            </VariantItem>
            <VariantItem author="LeChuck" global applyAutomatically>
              Variant 3
            </VariantItem>
            <VariantItem author="LeChuck" readOnly>
              Read Only
            </VariantItem>
          </VariantManagement>
        </>
      );
    }}
  </Story>
</Canvas>

## Properties

<ArgsTable story="." />
