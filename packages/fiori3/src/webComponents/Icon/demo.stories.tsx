import React from 'react';
import { storiesOf } from '@storybook/react';
import { Icon } from '../../lib/Icon';

storiesOf('UI5 Web Components | Icon', module).add('Generated default story', () => (
  <Icon src={'generatedString'} onPress={null} />
));
